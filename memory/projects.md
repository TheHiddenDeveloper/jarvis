# Projects

- [2026-08-02] **Gileara business platform** — ~/Dev/gileara-biz-platform. Active git repo; business service catalog & pricing docs in home dir (Gileara_MSME_Service_Catalogue docs).
- [2026-08-02] **Jarvis** — this assistant. Foundation (brain + CLI + desktop hands) built 2026-08-02. Roadmap: voice, remote web UI, scheduled autonomy pending.

- [2026-08-02] - [2026-08-02] Jarvis memory integration with Ideaverse vault done: canonical brain = ~/Ideaverse (Obsidian), fast layer = ~/jarvis/memory/. Next scaling pillars: semantic recall (embeddings), scheduler/autonomy, phone HTTPS reach, observability.

- [2026-08-02] - [2026-08-02] Pillar 1 complete: memory = vault_read/vault_search (keyword) + vault_search_semantic (embeddings, meaning-based). Next pillar: scheduler/autonomy (Pillar 2).

- [2026-08-02] 2026-08-02 LATENCY FIX: daemon had a session-resume hang. Session ses_03db88da5 was created from $HOME (dir=/home/thehiddendeveloper) but daemon spawns opencode with cwd=~/jarvis/server; `opencode run -s <sid>` HANGS (≥60s, opencode 1.18.11 bug) when cwd != session.directory. Fix in server/daemon.js runAgent: resume with RESUME_TIMEOUT_MS=60s; if empty output, unlink server-session.id and retry fresh. Deleted stale session id, restarted jarvis-daemon.service. Verified: 180s → 11.7s (agent 9.4s, tts 2.4s). Added [utter]/[transcribe] stage timing logs.

- [2026-08-02] 2026-08-02 LATENCY PROFILE (healthy): transcribe 3.3s (faster-whisper base model load dominates; inf is fast), agent 9.4s (opencode proc boot ~8s + LLM ~1-2s; MCP init adds ~0), TTS 2.4s (piper load + synth). Voice total ~15s. Big levers: (A) warm whisper+piper daemons saves ~5.5s, (B) daemon talks to `opencode serve` HTTP API directly (POST /session + POST /session/{id}/message, OpenAPI at /doc, warm server on 127.0.0.1:4096) eliminates ~8s proc boot, (C) streaming reply for sub-2s first audio, (D) VAD in voice-loop (fixed 6s window today). opencode `--attach` client still boots fully (~11s) = NOT a win.

- [2026-08-02] 2026-08-02 LATENCY PROJECT DONE (A+B approved + built). Speech server: scripts/speech-server.py (:7888) loads whisper base + piper once; /transcribe (wav bytes) ~1.4s, /speak ~0.2s (was 3.3s/2.3s). piper AudioChunk uses audio_int16_bytes. opencode warm: daemon spawns `opencode serve --port 4096` (password in state/opencode-server.password, basic auth user 'opencode'), runAgent does POST /session + POST /session/{id}/message {agent:'jarvis', parts:[{type:'text',text}]}, extracts parts[].type=='text'. CLI spawn is fallback only. MEASURED: text 2.4s, voice 3.7s conversational (was 180s bug / 15s healthy). LLM call itself ~2-7s (deepseek-v4-flash-free via opencode build); the ~7s variance is provider, tool-using turns add more. Next idea: stream reply sentence-by-sentence for first-audio-in-~1s.

- [2026-08-02] 2026-08-02 REPO SYNC: ~/jarvis is the LIVE working tree but its git repo has ZERO commits/no remote. ~/Dev/jarvis is the PUBLISHED repo (origin github.com/TheHiddenDeveloper/jarvis.git, master). User decision: keep ~/Dev/jarvis canonical; rsync live changes there + commit + push (exclusions: state/ venv/ models/ widget/target/ node_modules/ .env __pycache__). Synced + committed f6d4d97 "feat: Obsidian memory recall + warm-server latency overhaul" and pushed (ee5e584..f6d4d97). Remember to re-sync ~/jarvis -> ~/Dev/jarvis before any future git push.

- [2026-08-02] [2026-08-02] LATENCY PROJECT PART 2 (streaming → lean voice fast-path). Verified opencode serve v1.18.11 does NOT expose incremental assistant-text streaming over HTTP (blocking POST returns full blob at completion even with Accept: text/event-stream + format {type:"text"}; /api/event = user echoes only; /global/event = message metadata, empty text parts; live UI streaming is internal WebSocket). User chose "1+2": (1) lean jarvis-voice agent on its own warm session (template at server/opencode-agents/jarvis-voice.md, installed to ~/.config/opencode/agents/, replies TASK if action needed) + pattern classifier routes tasks to full jarvis; both sessions warmed at boot via message+revert. (2) reflex chime: ffmpeg two-tone → state/chime.wav, served /chime.wav, played client-side on submit. Measured: chit-chat ~2.2-2.9s (model floor ~2s, nvidia models slower 13-60s), tasks 8-20s, TASK escalation works (read-my-notes → real vault read), voice round-trip 3.7s. Synced to ~/Dev/jarvis, committed 8c09096, pushed. Re-sync before any future push.

- [2026-08-02] [2026-08-02] CORRECTION: opencode serve DOES stream assistant text — via the `/global/event` SSE channel (no auth beyond Basic). Verified live: during a generation it emits `message.part.delta` + `message.part.updated` events carrying incremental text (27 deltas in one run; payload shape: {payload:{type:"message.part.delta",properties:{sessionID,messageID,partID,field:"text",delta:{...}}}}). My earlier "no streaming" conclusion was wrong (I had probed the wrong channels/paths). The blocking POST /session/{id}/message still returns one final JSON blob at completion, but the SSE channel streams tokens in parallel. Caveat: measured TTFT +10.4s vs completion +10.8s with deepseek-v4-flash-free — the model/proxy delivers text in ONE burst at the end, so wiring up deltas buys ~nothing until we use a provider that genuinely streams early (TTFT << total). Chime remains the perception bridge.

- [2026-08-02] [2026-08-02] BUILT live reply streaming. Daemon keeps a persistent SSE subscription to opencode /global/event (message.part.delta, field=text, properties.delta=string chunk) matched by sessionID via genWatchers map; /api/ask + /api/mic/stop now respond as SSE: {type:user} → {type:delta}* → {type:done {reply,audioB64}} → {type:error}. Client (app.js streamPost) reads fetch stream and renders deltas into the status area live; chime still plays on submit; TTS/speech only on final reply. Streaming is wired only on the full-jarvis task path (chit-chat fast path is 2s and bursts anyway — no deltas streamed there, avoids TASK-flash on escalation). Verified: task "search the web for what time it is in tokyo" → first delta +3.58s, 310 deltas; streamed text matches final reply. NOTE: current model bursts tokens at end so live text shows the agent's interim narration then the answer; sentence-level TTS deferred (pointless until a provider streams early). Committed 438092b, pushed. Next for realtime: find a genuinely streaming provider, then add sentence-level TTS.

- [2026-08-02] [2026-08-02] STABILITY HARDENING committed 70dd452 + pushed. (1) Fixed stream-accumulation bug: daemon now accumulates deltas per-generation (handleUtteranceStream) so client shows growing text, not last chunk; correct across SSE reconnects. (2) Single-flight: global utteranceChain queue serializes turns (verified: concurrent task+chitchat ran serialized, 12.8s total, no overlap). (3) Circuit breaker (createBreaker, 3 fails → 15s open) + 5s health memo on spawnOcServer/ensureSpeech; request path fails fast with ServiceUnavailableError; background connectOcEvents uses spawnOcServer({force:true}) so crashed server still auto-respawns. (4) Generation watchdog: FAST_REPLY_MS=12s (fast agent) / FIRST_DELTA_MS=25s (full agent, first-delta); on timeout aborts POST via AbortSignal.any + calls POST /session/{sid}/abort (verified: returns true, session recovers, next turn works); throws GenerationTimeoutError; fast agent returns graceful canned "I got stuck" line, full agent propagates to friendlyError. (5) Degradation tiers: TTS failure → text-only reply (client skips audio); friendlyError maps timeout/unavailable to clean messages. Verified live: watchdog with 600ms budget returned canned reply <1s; streaming accumulation maxLen=302 growing. Env knobs JARVIS_FIRST_DELTA_MS/JARVIS_FAST_REPLY_MS.

- [2026-08-02] [2026-08-02] SPEED WORK shipped (daemon changes live, sync/commit pending). (1) DETERMINISTIC COMMAND FAST-PATH: local reply table (DET_COMMANDS + matchDeterministic) for time/date/greeting/thanks/who-are-you, checked BEFORE task routing. Time/date previously hit TASK_RE → full agent 8-20s; now ~170ms total (llm 0-36ms + TTS). Regexes kept narrow so they can't shadow real asks — tested "the date of my flight is friday" (must NOT give today's date) and "what time is my meeting tomorrow"; fixed over-greedy bare "the date"/"the time" alternatives. (2) SEMANTIC REPLY CACHE: speech-server.py gained POST /embed (fastembed BAAI/bge-small-en-v1.5, lazy-loaded, 384d, ~8ms warm, first call ~0.7s). Daemon cacheLookup/cacheStore persist ~/jarvis/state/reply-cache.json (mode 600, cap 200 LRU by ts, TTL 7d JARVIS_CACHE_TTL_MS, sim threshold 0.9 JARVIS_CACHE_MIN_SIM). Only consulted on chit-chat branch (never tasks/side effects). Hit measured: llm=12-30ms, total ~1-1.5s (TTS-bound) vs 4s miss. All failures degrade to miss. (3) SESSION COMPACTION: POST /session/{sid}/summarize {providerID,modelID,auto:false} (session model from GET /session/{id}: opencode/deepseek-v4-flash-free). maybeCompactSession counts messages (paged GET /message?limit=100, X-Next-Cursor) and compacts at boot if >80 (JARVIS_COMPACT_MIN_MESSAGES, JARVIS_COMPACT=0 disables). Validated: voice session summarize 9.7s, session still answered after; main session 139msgs compacted in 22s. Main session was heavy (~24M char estimate, 139 msgs, big tool outputs). (4) DEGENERATE-REPLY RECOVERY: web-search turns intermittently end with bare "..." final message (no JSON envelope) → speak("...")/empty audio. isDegenerateReply (empty/punct-only/fallback). runJarvisVoice: on degenerate → short reformat follow-up reusing session context (seconds, not full re-run); if still degenerate → graceful line. runFastVoice: degenerate → escalate to TASK. Sample: 7/7 web-search runs proper after fix. Streaming path unchanged (still full-jarvis task only). daemon healthy, JARVIS_DEBUG logs [cache] HIT/store sim, agent timings.

- [2026-08-02] [2026-08-02] UI IMPROVEMENTS round 1 (user picked 3 items, all built + verified). (1) CANCEL/STOP: new daemon POST /api/abort (daemon.js). Track in-flight generation via module-level currentAbort = {ctrl, sid, cancelled} set in runAgentWarm; /api/abort sets cancelled, aborts local fetch ctrl AND POSTs /session/{sid}/abort server-side. New GenerationCancelledError propagates past runAgent + runFastVoice (a cancel must NOT trigger CLI fallback or TASK escalation) and the /api/ask + /api/mic/stop routes reply {type:"cancelled"} on the SSE stream. Client: Stop button (header) + Esc-to-stop call stopCurrent() → POST /api/abort; streamPost returns {cancelled:true} on the cancelled event (or if stream dies while stopRequested); ask/sendAudio/sendHostAudio render "Stopped" via resetAfterStop(). Verified live: 4s into a web-search task, abort → {type:"cancelled"} on the stream, session recovered cleanly, follow-up turn worked. (2) WINDOW CONTROLS: widget tauri.conf.json withGlobalTauri:true; capabilities/default.json now includes core:window:allow-minimize/allow-unminimize/allow-close (was only allow-start-dragging); app.js adds window.__TAURI__ guard — winMin/winClose buttons call getCurrentWindow().minimize()/close(); CSS .win only shows with .has-tauri (so plain-browser view hides them). Widget rebuilt (cargo build --release) + service restarted. (3) PERSISTENT HISTORY: app.js now persists {who,text,ts} to localStorage jarvis_log (cap 400 entries, text-only — audio stays ephemeral), restores on load, Clear button (loghead) wipes it + hides when empty. Playwright-verified end-to-end: stop-during-thinking → "Stopped", clear → 0 msgs, reload → messages restored. Deferred/known: sentence-level TTS still waiting on a genuinely early-streaming provider. Sync+commit+push pending (this entry written before sync).

- [2026-08-02] 2026-08-02 — Shipped live progress feed in widget (commit f476624). Tools enabled on task path; daemon polls in-flight session message every 250ms (opencode streams no tool events), maps each tool call to a friendly label ("Searching the web — \"<query>\"", "Reading your notes", etc.) relayed as {type:activity} SSE events; widget shows a pulsing activity line under Thinking… until the reply lands. Key fixes found empirically: (1) message list must be scanned fully, not just msgs[0] — compaction/system messages sit ahead of the in-flight assistant message; (2) tool part input is empty {} at "pending" status — wait for it to populate so queries display; (3) 600ms polling missed fast vault_read calls, 250ms catches them. Verified end-to-end via curl SSE + Playwright DOM sampling: Thinking… → Searching the web — "USD to GHS exchange rate today" → clean reply → speaking. Abort regression: user→delta→activity→cancelled, no CLI fallback. Widget loads assets live from daemon :7878 (frontendDist is a URL), so no rebuild needed — just restart the widget unit to refresh the WebView. TODO: streaming deltas leak the raw JSON envelope/thinking-out-loud into #you before extractReply cleans up — cosmetic, pre-existing.

- [2026-08-03] - [2026-08-03] ROUND 3 — collapsible thinking panel + markdown replies (shipped). Fixed the pre-existing leak where streamed deltas (raw reasoning + {"reply":...} JSON envelope) appeared in #you during a task. Now: streamed deltas feed a collapsible "Thinking" panel inline in the log; tool activity shows as a dot-sequence; the final reply renders as markdown in a clean bubble. Client rewrite in server/public/{app.js,index.html,style.css}: startLive/addLiveStep/setLiveRaw/buildThinkBlock/endLive, md() markdown renderer, streamPost routes user/delta/activity/done/cancelled/error (delta → collapsible <pre class="think-raw">, activity → dot-step, done → renderReply). ENVELOPE STRIP: renderReply strips a trailing {"reply":...} from the raw so the thinking panel shows reasoning only (regex /{"reply"\s*:/). Verify (Playwright, daemon :7878): .md bubble clean markdown, no leak into #you (stays "Thinking…" while live), thinking body toggles collapsed (click .think-head toggles .think-body.collapsed + .think-head.open), persisted localStorage jarvis_log restores collapsed thinking block + reply (note: use entry.text len, not .content). Also: a slow/bloated session can stall tasks — restart `systemctl --user restart jarvis-daemon` to trigger compaction (288 msgs) and warm; compaction done logs "[compact] jarvis daemon: done" + "[warm] jarvis session ready" before first live test. Widget still loads assets live from daemon (no rebuild). rsync bag: keep .playwright-mcp/ out (gitignore or rsync exclude).

- [2026-08-03] - [2026-08-03] MIC FIX + runtime-switchable mic selector (shipped). User reported "no speech detected" on recording. Root cause (NOT the round-3 thinking panel; git confirmed daemon.js untouched by that change): the daemon records via ffmpeg `-f pulse -i default`, and the Pulse default source (Logitech USB Headset mic) returns DIGITAL SILENCE (max_volume ≈ -62dB even at +20dB boost). The onboard mic `alsa_input.pci-0000_00_1f.3.analog-stereo` DOES produce signal (RMS ≈ -29 to -31dB). So recordings captured silence → Whisper → "no speech detected". Auto-probing can't reliably beat cold (SUSPENDED) PipeWire devices: a 1.2-3s cold probe returns -200dB for every source, so runtime auto-detect is only best-effort. Real fix: make the mic selectable. Added to daemon.js: persisted choice in ~/jarvis/state/mic-source.json (mode 600); micSetting() priority = env JARVIS_MIC_SOURCE > file > "auto"; resolveMicSource() returns explicit device as-is, probes only for "auto"; GET /api/mic/sources (current/setting/options incl auto+default+each alsa_input with state) and POST /api/mic/source {source} validate+persist+clear cache. Widget: #micSel dropdown in loghead (app.js loadMicSources on IS_LOCAL, POST on change; index.html + style.css .micsel). Seeded mic-source.json to the onboard device so recording works immediately. Verified: daemon records real audio from onboard (RMS -31dB peak -17.6dB), GET returns options, dropdoual switch auto<->onboard persists to disk live. Also fixed a latent bug: micProc now clears micProc on "exit" (external kill) + clears micTimer, so a killed capture won't wedge future /api/mic/start. NOTE for future: check `pactl list sources short`; keep .playwright-mcp/ gitignored.
